using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;
using System;
using UnityEngine.SceneManagement;
using UnityEngine.UI;
using UnityEngine.EventSystems;

/*
public class AssemblyTest {

    public Vector3 BasePosition = new Vector3(-1000, 3, -1000);

    // AssemblyGameDriver

    // ConveyorObject

    // ConveyorPickup

    // DropSlot

    [SetUp]
    public void SetUp() {


    }

    [UnityTest]
    public IEnumerator Test_GameDriver() {

        GameObject agd = new GameObject();
        agd.AddComponent<AssemblyGameDriver>();

        AssemblyGameDriver _agd = agd.GetComponent<AssemblyGameDriver>();

        _agd.rotationSpeed = 5f;
        _agd.TotalComponents = 2;

        // create / set conveyor system 

        yield return new WaitForSeconds(1.0f);
    }
}
*/